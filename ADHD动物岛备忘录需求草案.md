# ADHD 动物岛备忘录需求草案

## 1. 项目一句话

做一个“动物岛风格”的极简项目备忘录：允许一个 ADHD 式跳跃思维的人，在不同项目之间快速切换、随手乱写想法，并把乱写内容自动整理成可执行待办。

这个项目先做小，不一开始追求复杂的知识库。核心是：

- 快速创建项目
- 快速切换项目
- 快速输入想法
- 自动归纳行动点
- 用打字机效果生成每日或阶段性工作总结

## 2. 设计方向

视觉上使用 `animal-island-ui` 的温暖、圆润、游戏感组件，不直接复制官方素材。整体感觉是“温柔的小岛工具”，不是复杂生产力系统。

可用组件来源：

- 源码路径：`C:/Codex related/animal crossing source code/animal-island-ui`
- 组件库：React + TypeScript
- 可重点使用：
  - `Button`
  - `Card`
  - `Input`
  - `Tabs`
  - `Modal`
  - `Checkbox`
  - `Select`
  - `Typewriter`
  - `Phone`
  - `Time`

设计关键词：

- 温暖
- 简洁
- 不压迫
- 像小动物在和你慢慢说话
- 项目很多也不显得乱
- 用按钮、卡片、标签切换来减少认知负担

## 3. 用户核心问题

现在普通备忘录的问题：

- 所有想法混在一起，很难按项目分
- ADHD 思维会在不同项目之间频繁跳跃
- 想法刚冒出来时通常不是完整任务，而是一段散乱的话
- 整理待办本身也会变成额外负担
- 回顾工作时容易不知道今天到底做了什么、下一步是什么

这个工具要解决的是：

- 允许乱写
- 允许跳跃
- 系统负责帮忙归类、整理、提炼下一步

## 4. MVP 功能范围

第一版只做这些：

1. 项目列表
2. 创建项目
3. 项目内备忘录输入
4. 从一段乱写内容中提炼待办
5. 待办勾选
6. 项目工作总结
7. 打字机式总结展示

暂时不做：

- 登录
- 云同步
- 多人协作
- 日历排期
- 复杂标签系统
- 复杂 AI 工作流
- 语音输入

语音可以作为第二阶段。第一阶段先做文字输入，因为它更简单，也能先验证核心体验。

## 5. 页面结构

### 5.1 首页 / 项目岛屿

用途：显示所有项目，让用户快速进入不同项目。

内容：

- 顶部显示当前时间或日期
- 项目卡片列表
- 新建项目按钮
- 每个项目显示：
  - 项目名
  - 一句项目描述
  - 未完成待办数量
  - 最近更新时间

交互：

- 点击项目卡片进入项目详情
- 点击新建项目打开创建项目界面

视觉建议：

- 项目像一个个小岛卡片
- 不要做得太花
- 卡片数量多的时候要能快速扫视

### 5.2 创建项目界面

用途：用很轻的方式创建一个项目。

字段：

- 项目名称
- 项目一句话描述
- 项目颜色或图标

可选默认文案：

- 项目名称 placeholder：`这座小岛叫什么？`
- 项目描述 placeholder：`它现在主要是关于什么？`
- 创建按钮：`开岛`

创建后：

- 回到项目详情页
- 自动创建一个空的想法输入区

### 5.3 项目详情页

用途：一个项目的主工作区。

区域：

- 项目标题
- 想法输入区
- AI 整理按钮
- 整理出的待办列表
- 原始想法记录
- 工作总结入口

核心交互：

1. 用户在输入框里乱写一段话
2. 点击 `帮我整理`
3. 系统从文本里提炼行动点
4. 用户确认后加入当前项目的待办列表

示例：

用户输入：

```text
啊我今天这个项目好复杂但是我知道我需要先把 a 做了然后可能会过一会做 b？
```

系统输出：

```text
- 做 a
- 做 b
```

### 5.4 工作总结页 / 晨会对话

用途：把项目当前状态整理成一段像小动物晨会一样的温柔总结。

内容来源：

- 今天新增的想法
- 今天完成的待办
- 仍未完成的待办
- 最新一条原始想法

展示方式：

- 使用 `Typewriter` 打字机组件逐字出现
- 文案像一个温柔的小岛居民在帮你总结

示例：

```text
早上好！今天这座小岛的重点是先把 a 做完。
昨天你已经整理出了 b，不过它还没有完成。
如果现在脑子有点乱，我们就先从最小的一步开始：打开 a 的文件。
```

按钮：

- `重新生成`
- `复制总结`
- `回到项目`

## 6. 数据结构草案

### Project

```ts
type Project = {
  id: string;
  name: string;
  description?: string;
  color?: string;
  createdAt: string;
  updatedAt: string;
};
```

### Note

```ts
type Note = {
  id: string;
  projectId: string;
  rawText: string;
  extractedTaskIds: string[];
  createdAt: string;
};
```

### Task

```ts
type Task = {
  id: string;
  projectId: string;
  title: string;
  done: boolean;
  sourceNoteId?: string;
  createdAt: string;
  completedAt?: string;
};
```

### Summary

```ts
type Summary = {
  id: string;
  projectId: string;
  text: string;
  createdAt: string;
};
```

第一版可以先存在浏览器本地 `localStorage`。等功能顺了，再考虑数据库或云同步。

## 7. “胡言乱语整理”功能

### 第一版：规则 + 简单 AI 接口预留

先做一个前端函数，把用户输入转成候选任务：

- 识别 `先把...做了`
- 识别 `做 a`
- 识别 `然后...做 b`
- 识别 `需要...`
- 识别 `应该...`
- 按句号、逗号、问号、换行切分

第一版规则提取不需要完美。目标是先把交互跑通。

### 第二版：接入 AI 总结

后面如果要更准，可以加一个 AI 整理接口。

输入：

```json
{
  "projectName": "项目名",
  "rawText": "用户乱写的一大段话"
}
```

输出：

```json
{
  "tasks": ["做 a", "做 b"],
  "summary": "这段话主要是在说先处理 a，再处理 b。"
}
```

AI 提示词目标：

- 不批判用户表达
- 不补充用户没有说过的任务
- 把任务写成短句
- 每条任务尽量以动词开头
- 不超过 5 条，除非用户明确列了更多

## 8. 语音功能想法

语音先不做 MVP。

原因：

- 浏览器语音识别兼容性需要单独处理
- 如果要转录服务，可能涉及 token 或费用
- 当前最重要的是验证“乱写 -> 任务”的体验

后续可以做成：

- 一个麦克风按钮
- 用户说话后自动填入输入框
- 用户仍然点击 `帮我整理`

语音不是独立功能，它只是更快的输入方式。

## 9. 推荐开发步骤

### Step 1：项目初始化

目标：

- 在当前目录创建 React + TypeScript + Vite 项目
- 引入 `animal-island-ui`
- 确认基础页面能跑起来

验收：

- 浏览器能看到一个动物岛风格首页
- 至少有一个按钮和一个卡片

### Step 2：项目列表和创建项目

目标：

- 可以创建项目
- 可以看到项目卡片
- 可以进入项目详情

验收：

- 创建项目后刷新页面仍然存在
- 项目卡片能显示名称、描述、未完成数量

### Step 3：项目详情和想法输入

目标：

- 每个项目有自己的输入区
- 输入的原始想法保存在项目下

验收：

- 在 A 项目写的内容不会出现在 B 项目
- 刷新后记录仍然存在

### Step 4：乱写内容提炼待办

目标：

- 点击 `帮我整理`
- 系统生成候选待办
- 用户确认后加入待办列表

验收：

- 示例句子能提炼出 `做 a` 和 `做 b`
- 待办可以勾选完成

### Step 5：工作总结和打字机效果

目标：

- 根据项目数据生成一段工作总结
- 使用 `Typewriter` 逐字显示

验收：

- 总结包含当前项目名、完成项、未完成项
- 点击重新生成可以重新播放打字机效果

### Step 6：体验打磨

目标：

- 简化视觉
- 减少页面干扰
- 调整移动端布局

验收：

- 手机宽度下也能舒服使用
- 不会因为卡片、按钮、文字太多而变得压迫

## 10. 第一版界面文案

按钮：

- `开岛`
- `进入`
- `帮我整理`
- `加入待办`
- `今天总结`
- `重新生成`
- `复制`
- `完成`

空状态：

- `这里还没有项目。先开一座小岛吧。`
- `这个项目还没有想法记录。乱写一点也可以。`
- `还没有待办。你可以先把脑子里的东西倒出来。`

总结语气：

- 温柔
- 简短
- 不说教
- 不制造压力
- 像晨会一样帮用户确认下一步

## 11. 需要注意的体验原则

- 用户可以乱写，不需要整理好再输入
- 系统不要显示太多解释
- 项目切换必须快
- 待办不要太复杂
- 首页不要变成仪表盘
- 每次只引导用户看到“下一步”
- 不要把 ADHD 当成缺陷来设计，而是把跳跃思维当成正常输入模式

## 12. 下一步

下一步建议从 Step 1 开始：

创建一个最小 React 项目，把 `animal-island-ui` 接进来，先做出首页、项目卡片和创建项目弹窗。

